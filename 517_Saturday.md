打字把所有字母位置学会 
 第0关：
1、 SSH基础 - 学习了如何使用SSH命令连接到远程服务器
2、命令参数 - 了解了SSH命令的基本参数，如指定端口的-p参数
3、安全意识 - 体验了密码输入的安全措施（不显示输入内容）
使用SSH命令连接：
ssh bandit0@bandit.labs.overthewire.org -p 2220 
命令解释：
ssh：SSH客户端程序（远程登录程序）
bandit0@bandit.labs.overthewire.org：用户名@主机名
-p 2220：指定连接端口为2220（SSH默认端口是22）
第1关：
1、文件查看基础 - 学习了如何使用ls命令列出目录内容
# 基本用法 - 列出当前目录内容
ls

# 列出指定目录内容
ls /etc

# 长格式列出所有文件（包括隐藏文件）
ls -la

# 按大小排序（从大到小）
ls -lS

# 按时间排序并以人类可读格式显示
ls -lth
2、文件内容读取 - 学习了如何使用cat命令查看文件内容
# 基本用法 - 显示文件内容
cat readme

# 显示带行号的文件内容
cat -n readme

# 创建新文件并输入内容（按Ctrl+D结束）
cat > newfile.txt

# 将多个文件内容合并到一个新文件
cat file1 file2 > combined.txt

# 将文件内容追加到另一个文件
cat file1 >> file
3、Linux导航 - 了解了Linux文件系统的基本导航方法
1、pwd (你在哪？) 2、ls (看周围有啥？) 3、cd (到哪？)

第二关：
1、特殊字符处理 - 学习了如何处理以特殊字符（如破折号）命名的文件
方法1：使用相对路径前缀./
cat ./-
方法2：使用绝对路径
cat /home/bandit1/-
2、路径表示法 - 了解了使用./前缀明确指示当前目录中的文件

第三关：文件名与特殊字符处理    

1、空格处理：掌握含空格文件名的处理方法
方法1：使用单引号
cat 'spaces in this filename'
方法2：使用双引号
cat "spaces in this filename"
方法3：使用反斜杠转义空格
cat spaces\ in\ this\ filename
1、引号使用：理解单引号 (' ') 与双引号 (" ") 的区别与应用
区别：想让里面的变量生效、命令执行 -> 用双引号 "。
     想让里面的所有东西都原封不动 -> 用单引号 '
应用：最常见的原因是，当你的文本里有空格或者特殊字符时。
3、转义字符：学会使用反斜杠 (\) 转义特殊字符
第四关：文件与目录基础
1、隐藏文件：了解 Linux 隐藏文件概念 (以 . 开头)
～以点 . 开头的文件/文件夹在Linux中默认隐藏。
～目的是为了保持目录整洁，避免显示大量不常直接操作的配置文件。
～这是一种显示约定，不是安全机制。
2、ls 高级用法：熟悉 ls 命令的常用选项 (如 -a, -l)
～ls -a：显示所有文件，包括隐藏文件（以.开头的文件）
～ls -l：使用长格式列出文件，显示详细信息（权限、所有者、大小等）
～ls -h：以人类可读的格式显示文件大小（KB、MB等）
～ls -al：组合选项，显示所有文件的详细信息
3、目录导航：熟练使用 cd 命令进行目录切换
cd 目录名 (进入)
cd .. (后退)
cd 或 cd ~ (回家)
cd / (去根)
cd /绝对/路径 (直达)
cd - (返回上一个)
第五关：
1、文件类型识别 - 学习了如何使用file命令确定文件的实际类型
  基本语法：file <文件名>
2、通配符使用 - 了解了如何使用通配符（如*）一次处理多个文件
# 列出所有.txt文件
ls *.txt

# 列出所有以"report"开头的文件
ls report*

# 列出所有包含"data"的文件
ls *data*

# 列出所有文件（包括隐藏文件，需要额外的-a选项）
ls -a *
3、ASCII与二进制 - 理解了人类可读文件（ASCII文本）与非人类可读文件（二进制数据）的区别
特性	ASCII文件	二进制文件
可读性	人类可读	通常不可读
内容类型	主要是文本	可以是任何类型的数据
编辑方式	任何文本编辑器	专用应用程序
存储效率	较低	较高
处理速度	较慢	较快
换行符表示	因操作系统而异（\n, \r\n, \r）	根据文件格式规范
文件大小	通常较大	通常较小（对于相同数据）
数据表示	基于字符编码	基于特定格式规范
跨平台兼容性	较好（注意换行符差异）	可能存在兼容性问题
第六关：
1、find命令基础 - 学习了如何使用find命令搜索符合特定条件的文件
 基本语法：find [起始目录] [选项] [表达式]
 起始目录：指定开始搜索的目录，默认为当前目录（.）
 选项：修改搜索行为的参数
 表达式：指定搜索条件
2、文件大小指定 - 了解了如何在find命令中指定文件大小
基本语法：find [路径] -type f -size [大小表达式]
 例子：# 查找大小正好是10MB的文件
     find /home -type f -size 10M
3、文件权限检查 - 学习了如何检查文件是否可执行
! -executable（！表示-not，不可执行）
4、组合搜索条件 - 了解了如何组合多个条件进行文件搜索
基本语法：find [起始目录] [选项] [表达式]
 起始目录：指定开始搜索的目录，默认为当前目录（.）
 选项：修改搜索行为的参数
 表达式：指定搜索条件
第七关：
1、所有权搜索 - 了解了如何根据文件的所有者和所属组搜索文件
find / -user bandit7 -group bandit6 -size 33c：
find：搜索文件的命令
/：从根目录开始搜索（搜索整个文件系统）
-user bandit7：指定文件所有者为bandit7
-group bandit6：指定文件所属组为bandit6
-size 33c：指定文件大小为33字节（c表示字节）
2、权限错误处理 - 学习了如何在大量权限错误消息中找到有用信息
使用2>/dev/null可以将错误消息重定向到"黑洞"，使输出更清晰：
find / -user bandit7 -group bandit6 -size 33c 2>/dev/null
第八关：
文本搜索基础 - 学习了如何使用grep命令在文件中搜索特定文本
大文件处理 - 了解了处理大型文本文件的有效方法
grep "millionth" data.txt：
grep：搜索文本的命令
"millionth"：要搜索的模式（字符串）
data.txt：要搜索的文件
在实际工作中，grep是处理日志文件和其他大型文本文件的必备工具

第九关：
1、使用sort命令对文本进行排序
sort data.txt：对data.txt文件的内容按行进行字典序排序
2、使用uniq命令识别唯一的行
uniq -u：只显示唯一的行（只出现一次的行）
uniq命令默认只比较相邻的行，所以在使用前需要先用sort命令对文件进行排序
uniq命令的常用选项包括：
-c：显示每行出现的次数
-d：只显示重复的行
-u：只显示唯一的行（只出现一次的行）
在实际工作中，sort和uniq命令常用于日志分析和数据处理
3、管道操作符|组合多个命令
sort data.txt | uniq -u
|：管道操作符，将sort命令的输出作为uniq命令的输入

第10关：
1、strings命令的用途和基本用法
strings命令用于从二进制文件中提取可打印的字符序列（文本字符串）。这对于检查二进制文件或包含非文本数据的文件非常有用。
strings data.txt：从data.txt文件中提取可打印的字符序列
2、grep命令过滤特定模式的文本
grep "^The" file.txt  # 匹配以"The"开头的行
grep "end$" file.txt  # 匹配以"end"结尾的行

第十一关：
Base64是一种将二进制数据转换为ASCII字符的编码方式，常用于在纯文本环境中传输二进制数据。它使用64个字符来表示数据，因此得名Base64。这64个字符包括：
大写字母A-Z（26个字符）
小写字母a-z（26个字符）
数字0-9（10个字符）
符号"+"和"/"（2个字符）
cat data.txt | base64 -d：
cat data.txt：显示data.txt文件的内容
|：管道操作符，将cat命令的输出作为base64命令的输入
base64 -d：使用Base64解码数据（-d选项表示解码）

第12关